import styled from 'styled-components';

const StyledError = styled.div`
  text-align: center;
  margin-top: 128px;

  p {
    color: red;
    font-weight: 600;

    font-size: 18;
  }
`;

export default StyledError;
